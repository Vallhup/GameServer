syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

message CS_INPUT_PACKET
{
	PacketHeader header = 1;
	Input key = 2;
	InputType inputType = 3;
}

message SC_MOVE_PACKET
{
	PacketHeader header = 1;
	Vec3 pos = 2;
}